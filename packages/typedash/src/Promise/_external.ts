export { andThen as then, otherwise as catch } from 'ramda'

export {
  allP as all,
  allSettledP as allSettled,
  isPromise,
  isThenable,
  rejectP as reject,
  resolveP as resolve,
} from 'ramda-adjunct'
